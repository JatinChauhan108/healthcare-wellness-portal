import express from 'express';

const router = express.Router();

// Placeholder - To be implemented by your friend
router.get('/', (req, res) => {
  res.json({ success: true, message: 'Reminder routes - Coming soon' });
});

export default router;
